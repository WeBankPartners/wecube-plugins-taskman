<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.webank.taskman.mapper.RequestInfoMapper">

    <resultMap id="RequestInfoMap" type="com.webank.taskman.domain.RequestInfo">
        <id column="id" property="id"/>
        <result column="request_temp_id" property="requestTempId"/>
        <result column="proc_inst_key" property="procInstKey"/>
        <result column="root_entity" property="rootEntity"/>
        <result column="name" property="name"/>
        <result column="reporter" property="reporter"/>
        <result column="report_time" property="reportTime"/>
        <result column="emergency" property="emergency"/>
        <result column="report_role" property="reportRole"/>
        <result column="attach_file_id" property="attachFileId"/>
        <result column="status" property="status"/>
        <result column="due_date" property="dueDate"/>
        <result column="status" property="result"/>
        <result column="description" property="description"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_time" property="createdTime"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_time" property="updatedTime"/>
        <result column="del_flag" property="delFlag"/>
    </resultMap>

    <resultMap id="RequestInfoDTOMap" type="com.webank.taskman.dto.resp.RequestInfoResq">
        <id column="id" property="id"/>
        <result column="request_temp_id" property="requestTempId"/>
        <result column="request_temp_name" property="requestTempName"/>
        <result column="proc_inst_key" property="procInstKey"/>
        <result column="root_entity" property="rootEntity"/>
        <result column="name" property="name"/>
        <result column="reporter" property="reporter"/>
        <result column="report_time" property="reportTime"/>
        <result column="emergency" property="emergency"/>
        <result column="report_role" property="reportRole"/>
        <result column="attach_file_id" property="attachFileId"/>
        <result column="status" property="status"/>
        <result column="due_date" property="dueDate"/>
        <result column="status" property="result"/>
    </resultMap>

    <select id="selectRequestInfo" parameterType="com.webank.taskman.dto.req.QueryRequestInfoReq"
            resultMap="RequestInfoDTOMap">
        SELECT ri.*,rt.name  request_temp_name
        FROM request_info ri
        INNER JOIN request_template rt ON rt.id = ri.request_temp_id
        <where>
            <if test="param.id !=null and param.id !=''">
               AND  ri.id = #{id}
            </if>
            <if test="param.requestTempId !=null and param.requestTempId !=''">
               AND  ri.request_temp_id = #{requestTempId}
            </if>
            <if test="param.procInstKey !=null and param.procInstKey !=''">
               AND  ri.proc_inst_key = #{procInstKey}
            </if>
            <if test="param.rootEntity !=null and param.rootEntity !=''">
               AND  ri.root_entity = #{rootEntity}
            </if>
            <if test="param.name !=null and param.name !=''">
               AND  ri.name LIKE CONCAT('%',#{name},'%')
            </if>
            <if test="param.reporter !=null and param.reporter !=''">
               AND  ri.reporter LIKE CONCAT('%',#{reporter},'%')
            </if>
            <if test="param.status !=null and param.requestTempId !=''">
               AND  ri.status = #{status}
            </if>
            <if test="param.description !=null and param.description !=''">
                AND  ri.description LIKE CONCAT('%',#{description},'%')
            </if>
            <if test="param.conditionSql!=null and param.conditionSql !=''">
                AND ${param.conditionSql}
            </if>
        </where>
        ORDER BY ri.updated_time DESC
    </select>

    <select id="selectSynthesisRequestInfo" parameterType="com.webank.taskman.dto.req.SynthesisRequestInfoReq" resultMap="RequestInfoMap">
        SELECT ri.*
        FROM
        request_info ri
        WHERE
        <if test="param.status !=null and param.status !='' ">
            ri.`status`=#{param.status}
            and
        </if>
        (
        SELECT COUNT(1)
        FROM role_relation rr
        WHERE
        ri.request_temp_id = rr.record_id
        AND
        rr.role_type =0
        AND
        <foreach item="roleStr" index="index" collection="param.roleName.split(',')" open="(" close=")" separator="OR">
            MATCH(rr.role_name)
            AGAINST(#{roleStr})
        </foreach>
        ) > 0
    </select>

</mapper>
